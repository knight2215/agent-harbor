{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability set for the main window. Grants only the core defaults required for the window to run plus the single `app_version` command exposed in Phase 0.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:allow-app-version"
  ]
}
