{
  "id": "FEAT-002",
  "type": "feat",
  "description": "Wire routing into the end-to-end message pipeline (task P4.6, architecture.md Section 2.2): fill in the orchestrator-core/src/pipeline.rs placeholder to assemble message -> route -> provider -> tool loop -> persist. Build a RoutingRequest from conversation/persona/tags/override, resolve a RoutingDecision via the routing PolicyRegistry, resolve the provider adapter from the ProviderRegistry, stream the response, run the Phase 3 tool loop (attach MCP tools, handle tool calls with permission gating, feed results back, continue), emit MessageDelta/MessageComplete/MessageError CoreEvents, and persist the assistant message + RouteMetadata + TokenUsage on completion. Add an orchestrator-core integration test that runs a message end-to-end through routing to a MOCK provider and the fixture MCP server, asserting persisted route metadata + rationale and that the tool loop + permission gating work. Also add a `send_message(conversationId, content, overrideRoute?)` Tauri command (Section 8.1) that drives the pipeline and streams via Tauri events, plus a minimal typed frontend IPC wrapper - NO chat UI (Phase 5). Finally flip README Phase 4 status to DONE. Depends on FEAT-001 (routing API).",
  "status": "completed",
  "steps": [
    "Precondition: FEAT-001 is complete on the phase-4-routing-pipeline branch (routing engine API exists and routing is an excluded crate). Continue on the same branch.",
    "orchestrator-core/Cargo.toml: it already path-deps routing/providers/mcp-client/domain/persistence. Confirm no new deps needed beyond what exists (serde/serde_json/uuid/chrono/thiserror/tokio; dev-deps async-trait/futures-util). If the pipeline needs futures-util in the NON-test build (to drive chat_stream's BoxStream via StreamExt::next), MOVE `futures-util = \"0.3\"` from [dev-dependencies] into [dependencies] (concrete literal, no workspace=true) and note it in the manifest comment. Keep the [[bin]] tool-loop-mock-server entry.",
    "orchestrator-core/src/pipeline.rs (P4.6): implement the pipeline assembling architecture.md Section 2.2. Define a `Pipeline` (or `run_turn`) entry that takes: the SessionManager, a routing PolicyRegistry (or resolved active policy), a providers::ProviderRegistry, the set of connected mcp_client::McpServerHandle for the conversation, a PermissionGate/PermissionRegistry, the CoreEvent UnboundedSender, plus the conversation id and the new user message content and optional per-message ManualRoute override. Flow: (1) append the user Message via SessionManager; load the Conversation (privacy_tags, conversation_pref, persona_id) and its persona (via SessionManager.personas().get) if any. (2) Build providers::ChatMessage history from the conversation's messages (map domain Role/MessageContent::Text to providers ChatMessage; include the persona system_prompt as a leading System message when present). (3) Build a routing::RoutingRequest { messages, privacy_tags from the conversation, persona, manual_override (the arg), conversation_pref (from the conversation), available: from providers::list_available_models OR a supplied Vec<AvailableModel>, budget: None for now }. Resolve routing::RoutingDecision via the PolicyRegistry resolve(). On RoutingError, emit MessageError and persist an Error-status assistant message, then return. (4) Resolve the adapter: registry.get(&decision.provider_id) -> Arc<dyn ChatProvider>; if absent, emit MessageError + persist Error message + return. (5) Build providers::ChatRequest { model: decision.model, messages, temperature/max_tokens from persona ModelParameters, stream: true, extra: Null }. If a ToolBridge/servers are present AND the chosen model capabilities.tools, attach_tools(&mut request) (capability negotiation: omit tools when the model is tool-incapable, per Section 4.4). (6) Create a Pending/Streaming assistant Message id up front. Run the TOOL LOOP: call provider.chat_stream (fall back to chat + synthesize one delta if !capabilities.streaming), accumulate ChatDelta content emitting CoreEvent::MessageDelta per chunk, and assemble tool_calls from deltas. When the turn finishes with tool calls (finish_reason ToolCalls or accumulated tool_calls present), for each ToolCall run bridge.handle_tool_call (permission gating happens inside the gate), push the assistant tool-call message + resulting tool messages back onto request.messages, and CONTINUE the loop for another provider turn; cap iterations with a sane bound (e.g. 8) to avoid infinite loops (emit MessageError if exceeded). When the turn finishes without tool calls, it is the final assistant message. (7) On completion: build domain::RouteMetadata { provider_id, model, rationale, source } from the RoutingDecision, gather TokenUsage from the provider Usage if present, persist the final assistant Message (status Complete, route=Some, usage) via SessionManager.append_message, and emit CoreEvent::MessageComplete { status, route, usage }. Keep the module framework-agnostic (no Tauri). Add thorough //! docs (audit for doc_overindented_list_items). Derive Debug where tests need it; never Debug-format a trait object.",
    "orchestrator-core/src/lib.rs: re-export the new pipeline public items (e.g. `pub use pipeline::{Pipeline, run_turn, PipelineError}` or whatever the implementation names them), keeping re-exports alphabetically ordered per convention.",
    "orchestrator-core/tests/pipeline_e2e.rs (new integration test, P4.6 acceptance): build an in-memory SessionManager (Db::open_in_memory), create a conversation, and drive a message END TO END through routing to a MOCK provider and the bundled tool-loop-mock-server (spawn via env!(\"CARGO_BIN_EXE_tool-loop-mock-server\") exactly like tests/tool_loop.rs). Use a scripted mock ChatProvider registered on a ProviderRegistry (via insert_instance) whose id matches the RoutingDecision's provider_id, and a routing PolicyRegistry whose available models include that mock as a candidate (construct AvailableModel rows so autoDefault selects it, or use a per-message manual override to make the decision deterministic - prefer exercising BOTH: one test asserts automatic routing selects the mock and records source=Automatic + a non-empty rationale; another asserts a manual override records source=Manual). Script the provider: first turn returns a tool call for a namespaced fixture tool (server id + '__echo'), second turn returns final text. Assert: MessageDelta events were emitted, the tool loop invoked the fixture tool under PermissionMode::Allow (permission gating exercised), the final assistant Message was PERSISTED with status Complete, and its route metadata carries the expected provider/model + a non-empty rationale (and correct source). Add a variant asserting an Ask-mode permission blocks then unblocks via PermissionRegistry.resolve within the assembled pipeline. Do NOT use live network.",
    "tauri-app/src/state.rs: extend AppState so the pipeline can be driven. Add the fields the send_message command needs that are not already present: a routing::PolicyRegistry (Arc-wrapped, active policy read from AppConfig.active_routing_policy at initialize, defaulting to autoDefault), and (reuse existing) session_manager, secret_store, core_events sender, permission_registry. If the provider registry / MCP server handles need to live on AppState for the pipeline, add them as Arc fields (build the provider registry from persisted ProviderConfig rows at initialize via providers::build_registry, mirroring list_available_models_inner; MCP server handles may be an empty/optional set for now since server-manager wiring is Phase 5 - document that the pipeline tolerates zero connected servers, in which case no tools are attached). Update AppState::initialize and AppState::new accordingly, keeping the existing constructor callers/tests compiling.",
    "tauri-app/src/commands.rs: add a `send_message` #[tauri::command] with the Section 8.1 signature send_message(conversationId: String, content: String, overrideRoute: Option<ManualRoute>) -> Result<(), CommandError>. Validate conversationId (parse_uuid), content (non-empty, bounded by a new MAX_MESSAGE_LEN e.g. 131072), and overrideRoute (validate_manual_route when present). Factor the body into a `send_message_inner` (the established _inner testability pattern) that assembles the pipeline inputs from AppState (session_manager, routing PolicyRegistry, provider registry, permission_registry/gate, core_events sender, MCP handles) and drives the pipeline turn, streaming via the existing core_events channel (already bridged to Tauri events by events.rs / spawn_core_event_bridge). The command returns after kicking off/awaiting the turn as appropriate (streaming happens via events, per Section 2.2 'streaming delivered via events, not by holding a command open' - if the turn is awaited, ensure it does not block other conversations; a spawned task driving the pipeline is acceptable and matches Section 7.5 concurrency). Add unit tests via send_message_inner: rejects invalid conversationId/empty content/over-long content before touching the core (mirror set_provider_secret_inner tests); a happy-path test may be light given the offline constraint (heavier e2e coverage is the orchestrator-core integration test).",
    "tauri-app/src/lib.rs: register `commands::send_message` in the tauri::generate_handler! invoke_handler list (this is what authorizes the command across IPC in Tauri v2). Keep the list ordering consistent.",
    "frontend/src/ipc/commands.ts: add a typed `sendMessage(conversationId: string, content: string, overrideRoute?: ManualRoute | null): Promise<void>` wrapper over invoke(\"send_message\", { conversationId, content, overrideRoute }), importing ManualRoute from ../types. Match the existing wrapper style/JSDoc. Do NOT add chat UI (Phase 5). Ensure the file passes prettier.",
    "frontend/src/ipc/commands.test.ts: add a minimal vitest case for sendMessage mirroring the existing command tests (assert it calls invoke with the right command name + args shape). Keep consistent with the existing test structure.",
    "frontend/src/types/index.ts: no new type is strictly required (ManualRoute already exists), but if send_message's overrideRoute or any new event field needs mirroring, add it keeping EXACT sync with the Rust serde shape (single-line unions if <=100 cols). Verify nothing drifted. (RouteMetadata/TokenUsage/MessageComplete already mirrored.)",
    "README.md: flip the 'Implementation status' Phase 4 line from PENDING to DONE, matching the DONE style of Phases 0-3 (a short one-paragraph summary: routing policy engine + full message pipeline - RoutingPolicy trait, complexity/privacy/cost signals, auto-default + manual-override precedence, PolicyRegistry, and the end-to-end message->route->provider->tool-loop->persist pipeline with the send_message command). Update the leading 'Phases 0 through 3 are implemented' sentence to 'Phases 0 through 4'. Keep the routing crate description note consistent with it now being an excluded/CI-verified crate (update the 'routing ... offline member' mention in the repo layout + sandbox sections to reflect that routing joined the excluded set in Phase 4).",
    "Pre-push self-checks: from hybrid-orchestrator run `cargo fmt --manifest-path crates/orchestrator-core/Cargo.toml --check` and `cargo fmt --manifest-path crates/tauri-app/Cargo.toml --check` and `cargo fmt --manifest-path crates/routing/Cargo.toml --check`; fix formatting. From frontend/ run `NODE_OPTIONS= npx prettier --check .` (global prettier) and fix any drift. Self-audit all new orchestrator-core/tauri-app code + doc comments against the clippy -D warnings lint list in context.json environment_constraints (especially: no Debug-format of the Arc<dyn ChatProvider>/trait objects; per-variant serde rename_all if any new tagged enum is added; skip_serializing_if on new empty Vec/Option fields; doc_overindented_list_items in the pipeline //! docs)."
  ],
  "acceptance_criteria": [
    "orchestrator-core/src/pipeline.rs is no longer a placeholder: it builds a RoutingRequest from conversation/persona/tags/override, resolves a RoutingDecision via the routing PolicyRegistry, resolves the adapter from the ProviderRegistry, streams the response emitting MessageDelta, runs the Phase 3 tool loop with permission gating (attach tools when the model supports them, handle tool calls, feed results back, continue), and persists the assistant message + RouteMetadata + TokenUsage while emitting MessageComplete (or MessageError on failure).",
    "An orchestrator-core integration test runs a message end-to-end through routing to a MOCK provider and the fixture MCP server, asserting the persisted route metadata + rationale (and source) and that the tool loop + permission gating work in the assembled pipeline; no live network.",
    "A send_message(conversationId, content, overrideRoute?) Tauri command exists (Section 8.1 signature), validates its arguments, drives the pipeline, streams via the existing Tauri core-event bridge, and is registered in generate_handler!. A minimal typed frontend sendMessage IPC wrapper exists. No chat UI is added.",
    "README 'Implementation status' shows Phase 4 as DONE with a summary, the leading sentence says Phases 0 through 4, and the routing-crate offline/exclude description is consistent with Phase 4.",
    "Any TS mirror changes stay in exact sync with the Rust serde shapes and pass prettier; vitest includes a sendMessage test."
  ],
  "verification": [
    "OFFLINE: `cargo fmt --manifest-path crates/orchestrator-core/Cargo.toml --check`, `cargo fmt --manifest-path crates/tauri-app/Cargo.toml --check`, `cargo fmt --manifest-path crates/routing/Cargo.toml --check` all pass from hybrid-orchestrator.",
    "OFFLINE: from frontend/, `NODE_OPTIONS= npx prettier --check .` passes.",
    "CI-DEFERRED (network-required): `cargo build/clippy(-D warnings)/test --manifest-path crates/orchestrator-core/Cargo.toml` (including the new pipeline_e2e integration test) and `cargo build/clippy/test --manifest-path crates/tauri-app/Cargo.toml` pass on ubuntu + windows; `npm run lint`, `npm run format:check`, and `npm run test` (vitest, incl. the sendMessage test) pass in the frontend."
  ],
  "blocked_reason": null,
  "findings": "Implemented the full P4.6 pipeline. WHAT WAS DONE: (1) orchestrator-core/src/pipeline.rs is no longer a placeholder - added `run_turn(&TurnContext, conversation_id, content, Option<ManualRoute>) -> Result<Message, PipelineError>` plus `TurnContext`, `PipelineError`, and `MAX_TOOL_LOOP_ITERATIONS` (=8). Flow: persist user Message -> load Conversation+persona -> build provider ChatMessage history (persona system_prompt as leading System msg; only Text content is threaded, tool-call/result history is skipped and replayed via the live loop) -> build routing::RoutingRequest (privacy_tags, persona, manual_override arg, conversation_pref, ctx.available, budget=None) -> policies.resolve() -> registry.get(provider_id) -> ChatRequest with persona temperature/max_tokens and stream=capabilities.streaming -> attach ToolBridge tools ONLY when servers non-empty AND capabilities.tools (Section 4.4) -> bounded tool loop calling stream_turn (chat_stream when streaming, else single chat() synthesized into one delta), emitting CoreEvent::MessageDelta per chunk, accumulating streamed tool-call fragments by index, and on tool calls pushing assistant tool-call msg + bridge.handle_tool_call results back and continuing -> on completion persist Complete assistant Message with RouteMetadata (decision.into_route_metadata()) + TokenUsage and emit MessageComplete. run_turn wraps run_turn_inner so ANY error emits MessageError + persists an Error-status message. (2) lib.rs re-exports run_turn/PipelineError/TurnContext/MAX_TOOL_LOOP_ITERATIONS (alphabetized) and the crate doc updated. (3) Moved futures-util from [dev-dependencies] to [dependencies] (concrete literal) with a manifest note - the pipeline drives BoxStream via StreamExt::next. (4) tests/pipeline_e2e.rs: three #[tokio::test]s over an in-memory SessionManager + scripted mock ChatProvider (registered via insert_instance) + the bundled tool-loop-mock-server spawned via env!(CARGO_BIN_EXE_tool-loop-mock-server): automatic_routing_runs_tool_loop_and_persists_route (asserts MessageDelta + MessageComplete emitted, tool loop ran under PermissionMode::Allow, persisted Complete assistant msg with provider/model + non-empty rationale + source=Automatic + usage), manual_override_records_manual_source (source=Manual), ask_mode_blocks_then_unblocks_within_pipeline (Ask-mode PermissionRequested blocks the turn on a task until PermissionRegistry.resolve). (5) tauri-app/state.rs: AppState gained `policies: Arc<routing::PolicyRegistry>` (active id read from AppConfig.active_routing_policy at initialize, stale/unknown id tolerated) and `mcp_servers: Arc<Vec<Arc<McpServerHandle>>>` (empty for now - pipeline tolerates zero servers); AppState::new/initialize updated, existing callers/tests still compile. (6) commands.rs: added send_message #[tauri::command] (Section 8.1 signature: conversationId, content, overrideRoute?) + send_message_inner that validates (parse_uuid, non-empty/<=MAX_MESSAGE_LEN=131072 content, validate_manual_route), builds the provider registry + candidate models per-turn (mirrors list_available_models_inner), assembles TurnContext, and SPAWNS the pipeline turn (streaming via core_events, command returns immediately - Section 2.2/7.5). Added send_message_inner_rejects_invalid_input unit test. (7) lib.rs generate_handler! registers commands::send_message. (8) frontend commands.ts: added typed sendMessage wrapper; commands.test.ts: added a vitest case; no TS type change needed (ManualRoute/RouteMetadata/TokenUsage/MessageComplete already mirrored - verified). (9) README: Phase 4 flipped to DONE with a summary, leading sentence -> 'Phases 0 through 4', repo-layout + sandbox sections updated to reflect routing joining the excluded/CI-verified set in Phase 4 (members array now empty). OFFLINE VERIFICATION PASSED: `cargo fmt --check` for orchestrator-core, tauri-app, and routing all clean; `NODE_OPTIONS= npx prettier --check .` in frontend clean. CLIPPY SELF-AUDIT (deferred to CI, network-required): no unused imports (removed FinishReason after dropping the finish_reason field to avoid dead_code); TurnContext intentionally does NOT derive Debug (holds Arc<dyn ChatProvider>/trait objects - never Debug-formatted); pipeline //! doc list continuation lines indented to list-item content (doc_overindented_list_items); intra-doc links to non-imported items fully qualified (routing::RoutingDecision/RoutingError, providers::Capabilities::tools, crate::models::Conversation); no new serde enums; no new empty Vec/Option serde fields; used array/&expr where a Vec was unnecessary. CI-DEFERRED (INTEGRATIONS_ONLY, crates.io/npm 403): cargo build/clippy(-D warnings)/test for orchestrator-core (incl. pipeline_e2e) + tauri-app on ubuntu+windows, and frontend npm lint/format:check/test (incl. the sendMessage vitest case).\n\nREVIEW FIXES (2026-09-06-215816-review.md, NEEDS_CHANGES -> addressed on the pipeline side): (#4 fail-closed locality) TurnContext gained local_provider_ids: BTreeSet<String>, threaded into the RoutingRequest so routing enforces LocalOnly/Confidential on PROVABLE locality, never a zero price. tauri-app send_message_inner now derives it via a new local_provider_ids(configs) helper that classifies by concrete ProviderKind: LmStudio is always local; GenericOpenAI is local only when its base_url is a loopback host (localhost/127.0.0.1/[::1], via is_loopback_endpoint); every other kind is cloud - so a zero-priced cloud provider is NOT treated as local. New tauri unit tests: local_provider_ids_classifies_by_kind_and_endpoint, is_loopback_endpoint_recognizes_local_hosts. (#5 no test for the routing-error persistence path - FIXED) Added orchestrator-core/tests/pipeline_e2e.rs::routing_error_emits_message_error_and_persists_error_status: a LocalOnly conversation whose only candidate is a non-local cloud model (empty local set) drives run_turn to fail closed with PipelineError::Routing, asserting a CoreEvent::MessageError is emitted AND an Error-status assistant message (route=None) is persisted - guarding the pipeline's own error-and-persist contribution against regression. All existing TurnContext constructions in pipeline_e2e.rs updated with local_provider_ids. (#2 cost budget dead in the pipeline - JUSTIFIED, deferred with an explicit note) run_turn_inner still sets budget: None, now with a code comment documenting WHY: there is no persisted budget source in the Phase 4 data model (no CostBudget field on Conversation/AppConfig). The cost SIGNAL is fully wired+unit-tested in the routing crate and activates the instant a budget source is threaded; an absent budget is a valid 'no budget' signal, so a budget-less turn is correct until persona/config budgets land in a later phase. rustfmt --check clean for orchestrator-core + tauri-app; prettier --check clean for frontend (no frontend change needed - RoutingRequest/TurnContext are internal, never crossing IPC)."
}
