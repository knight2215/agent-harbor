{
  "project_type": "Rust multi-crate workspace (hybrid-orchestrator) + Tauri v2 desktop app with a Vite/React (TypeScript) frontend. This task is a build-fix on the embedded-inference-engine feature (Strategy B).",
  "language": "Rust (edition 2021) for the crates; TypeScript/React for frontend (not touched by this fix).",
  "build_system": "cargo, but the workspace has NO members: every crate is under [workspace] exclude in hybrid-orchestrator/Cargo.toml because each carries crates.io deps unreachable offline. CI builds each crate individually via `cargo build --manifest-path crates/<c>/Cargo.toml`. Frontend uses npm.",
  "test_framework": "cargo test per-crate (default features). Frontend: vitest. This fix's real proof is the routine `build-test` CI job (ubuntu+windows) going green.",
  "build_command": "Per-crate: `cargo build --manifest-path crates/<c>/Cargo.toml`. CANNOT run offline (crates.io 403). Native path: `cargo build --manifest-path crates/engine/Cargo.toml --features llama` (gated engine-native CI job only; needs C++ toolchain + network).",
  "test_command": "Per-crate: `cargo test --manifest-path crates/<c>/Cargo.toml`. CANNOT run offline.",
  "verification_instructions": "OFFLINE-ONLY verification available: (1) Structural manifest inspection - confirm crates/engine/Cargo.toml has NO `providers = { path = ... }` dependency, and crates/providers/Cargo.toml keeps `engine = { path = ... }` (one direction only, no cycle). (2) `cargo fmt --manifest-path crates/engine/Cargo.toml --check` and same for providers (fmt works offline, no network). (3) If ci.yml is touched: `ruby -ryaml -e 'YAML.load_file(\".github/workflows/ci.yml\")'`. (4) Frontend untouched, but if any frontend file changes: from frontend/, `NODE_OPTIONS= npx prettier --check .`. The default-feature compile passing and the llama native compile can ONLY be confirmed by CI (routine build-test job + gated engine-native job).",
  "snapshot_or_generated_files": "None relevant to this fix.",
  "setup_instructions": "Repo already cloned at /projects/agent-harbor. Feature branch `feat/embedded-inference-engine` is checked out, pushed, and is the head of open PR #25. Commit onto THIS branch. Do NOT push or create PRs (orchestrator handles that).",
  "environment_constraints": "INTEGRATIONS_ONLY sandbox: crates.io/npm return 403, no C++ toolchain. CANNOT run cargo build/clippy/test (need crates.io) or the llama.cpp native compile. CAN run: cargo fmt --check (offline), ruby YAML load, npx prettier. Verify the cycle break by MANIFEST INSPECTION, not by cargo. The routine `build-test` CI job is the only proof the default build compiles; the gated `engine-native` job is the only proof the native llama path compiles.",
  "contribution_requirements": "Keep clippy `-D warnings` discipline (no unused imports, no needless_lifetimes, etc.). Preserve serde camelCase on boundary enums. The fix must be structural (manifests + where the ChatProvider impl lives) with minimal code movement. Follow existing per-crate CI layout.",
  "key_patterns": "The `ChatProvider` trait + all OpenAI-shaped DTOs (ChatRequest/ChatResponse/ChatDelta/FinishReason/Capabilities/ModelInfo/ProviderError/ChatMessage/MessageRole/ChatChoice) live in crates/providers/src/contract.rs and are re-exported from providers' lib.rs. Adapters implement ChatProvider by wrapping a lower-level piece: e.g. adapters/native.rs `NativeAdapter` wraps `HttpSseClient`. The embedded adapter (adapters/embedded.rs) should mirror this: wrap the engine crate's lower-level inference API and do the OpenAI shaping in the adapter. EmbeddedFactory is registered in builtins.rs (register_factory, line ~182) and priced ZERO (always-local, line ~120); classified Embedded and surfaced under Local in the picker - THIS REGISTRATION MUST NOT CHANGE.",
  "relevant_files": [
    "hybrid-orchestrator/crates/engine/Cargo.toml (REMOVE the `providers = { path = \"../providers\" }` line; keep domain dep, llama-cpp-2 optional dep, `llama` feature)",
    "hybrid-orchestrator/crates/engine/src/lib.rs (REMOVE the `impl ChatProvider for EmbeddedEngine`; stop importing from `providers`; expose a provider-agnostic inference API returning plain token strings + a crate-local stop reason; keep EmbeddedEngine/EngineConfig/ModelEntry/registry logic and the constants; keep the native `mod native` gated by `llama` but have it return plain tokens, not OpenAI types)",
    "hybrid-orchestrator/crates/providers/Cargo.toml (KEEP `engine = { path = \"../engine\" }` and the `llama = [\"engine/llama\"]` passthrough - one direction only)",
    "hybrid-orchestrator/crates/providers/src/adapters/embedded.rs (MOVE the `ChatProvider` impl here: wrap the engine, build the prompt from ChatRequest messages, map engine tokens/stop-reason to OpenAI ChatResponse/ChatDelta/FinishReason; under default features return ProviderError::Other(NOT_COMPILED...) for chat/chat_stream; EmbeddedFactory::build returns Arc<dyn ChatProvider> of the wrapper)",
    "hybrid-orchestrator/crates/providers/src/contract.rs (reference: the ChatProvider trait + OpenAI DTOs - unchanged)",
    "hybrid-orchestrator/crates/providers/src/lib.rs (reference: re-exports; EmbeddedFactory re-export - unchanged)",
    "hybrid-orchestrator/crates/providers/src/builtins.rs (reference: EmbeddedFactory registration + ZERO pricing - MUST NOT CHANGE behavior)",
    "hybrid-orchestrator/crates/providers/src/adapters/native.rs (reference pattern: adapter wrapping a lower-level client)",
    ".github/workflows/ci.yml (reference: existing per-crate steps already order engine BEFORE providers; the gated engine-native job builds engine --features llama. NO change expected for Direction A)"
  ],
  "directory_structure": "hybrid-orchestrator/crates/{domain,engine,mcp-client,orchestrator-core,persistence,providers,routing,secrets,tauri-app}. engine = Strategy B embedded inference (llama.cpp behind default-OFF `llama` feature). providers = ChatProvider contract + adapters + registry. Frontend at hybrid-orchestrator/frontend."
}
