{
  "id": "FEAT-003",
  "type": "feat",
  "description": "Attach/upload file (incl. images) + Repository context as REAL working features, fully testable natively (no external network). Both feed bounded context into the NEXT chat turn by prepending a bounded context block to the user message `content` before send (the pipeline's `content` is a plain String that becomes the User Message text; this keeps run_turn and the streaming flow unchanged). ATTACH: a 📎 control opens the native file dialog (reuse tauri-plugin-dialog); text files have their contents read (new Tauri fs-read command) and included as context; images are included only if the selected model advertises the `vision` capability, else a clear 'this model doesn't support images' notice; attachments show as removable chips. REPOSITORY: a 📁 control picks a local folder and lets the user select a bounded subset of files to include as context, capped in total size, showing what's included. Attachments/repo files are cleared after a successful send (they applied to one turn).",
  "status": "completed",
  "steps": [
    "Backend: add a Tauri command `read_text_file(path: String) -> Result<FileContentView, CommandError>` in crates/tauri-app/src/commands.rs (with a factored `_inner` for tests) that validates the path is non-empty and within a max byte cap (reuse/introduce a MAX_ATTACH_BYTES const, e.g. 256 KiB per file), reads UTF-8 text via std::fs, and returns a display-safe view { path, name, byteLen, text }. Reject binary/oversized/unreadable files with a clear CommandError. For IMAGES, add `read_file_base64(path) -> Result<FileBinaryView, CommandError>` returning { path, name, mimeType, base64, byteLen } with its own cap (e.g. 4 MiB) and a mime guess from the extension. Register BOTH in crates/tauri-app/src/lib.rs generate_handler! (alongside the existing commands). Add unit tests for the _inner fns (temp files: happy path, oversized -> error, missing -> error, non-utf8 -> error).",
    "Backend: add a command `list_repo_files(dir: String) -> Result<RepoListing, CommandError>` (with `_inner`) that walks a picked directory NON-recursively-unbounded but SENSIBLY: cap the number of returned entries (e.g. 500) and skip common noise (.git, node_modules, target, dist, build, binary files by extension). Return { dir, files: [ { relPath, byteLen } ], truncated: bool }. This lets the UI present a checkbox list to pick a bounded subset; actual content is fetched per-file via read_text_file. Register it and add _inner unit tests (temp dir with a few files + a skipped dir; assert filtering + cap + truncated flag).",
    "IPC + types: add wrappers in frontend/src/ipc/commands.ts (readTextFile, readFileBase64, listRepoFiles) and camelCase TS types in frontend/src/types/index.ts (FileContentView, FileBinaryView, RepoListing with its RepoFileEntry) mirroring the Rust view structs. Keep the hand-mirror in sync exactly.",
    "Frontend attachments state: extend the composer (local component state is acceptable, or a small addition to the conversations store if the redesign needs it shared) to hold `attachments: Attachment[]` where each is { kind: 'text'|'image', name, path, byteLen, text? , base64?, mimeType? }. Wire the 📎 control (added as a slot in FEAT-002) to `open()` from @tauri-apps/plugin-dialog (multiple + a filter allowing text and image types), then for each picked path call readTextFile (text) or readFileBase64 (image). For an image, check the SELECTED model's capabilities.vision from the providers store `models` (find the AvailableModel matching the effective route/pin); if vision is false, do NOT attach the image and show a visible non-fatal notice 'This model doesn't support images'. Render attachments as chips with a remove (x) button (aria-label like 'Remove attachment <name>').",
    "Frontend repository picker: wire the 📁 control to `open({ directory: true })`, call listRepoFiles(dir), and present a compact selectable list (checkboxes) of files with a running total byte count and a hard cap (e.g. 128 KiB total across selected repo files); disable selection beyond the cap and show 'X of Y KiB included'. Selected files' contents are fetched via readTextFile and held alongside attachments (kind:'text', tagged as repo context). Show which files are included; allow removal.",
    "Send path: modify the composer submit so that, when attachments/repo files are present, it prepends a BOUNDED, clearly-delimited context block to the trimmed draft before calling sendMessage(content). Format e.g. a leading section: for each text/repo file, a fenced block '### Attached file: <name>\\n```\\n<text>\\n```', and for included images a note that an image named <name> is attached (image bytes handling: for THIS iteration, since run_turn maps only text content into the provider history, include the image as a base64 data note ONLY when vision is supported and document the limitation; do NOT silently drop it - either include it in the content block in a model-appropriate way or show the unsupported notice). Enforce the same total cap on the assembled block. Clear attachments/repo selections after a SUCCESSFUL send (mirror how the per-message override is consumed once).",
    "Do NOT change run_turn / send_message backend semantics: the assembled context travels inside the existing `content` String argument. (If, during implementation, passing large image base64 through content proves unworkable, prefer the text-context path for images-as-notes and clearly document that full multimodal image passing is a follow-up; keep the vision-capability gate + visible notice regardless.)",
    "CSS: add chip + repo-file-list styles to app.css using var(--ah-*) tokens.",
    "Tests: vitest for the composer attach flow (mock @tauri-apps/plugin-dialog open() returning a path or paths, mock the new invoke commands in routeInvoke with well-formed shapes): picking a text file adds a chip and its content is prepended to the sent content; picking an image with a vision-capable selected model attaches it; picking an image with a NON-vision model shows the 'doesn't support images' notice and does NOT attach; removing a chip drops it; attachments clear after send. vitest for the repo picker (mock open({directory:true}) + listRepoFiles): selecting files respects the cap and includes their content on send. Rust _inner unit tests as described. Every mounting test mocks ALL invoke commands (including the new ones) and initializes any new store fields in resets."
  ],
  "acceptance_criteria": [
    "A 📎 Attach control opens the native file dialog; selecting a text file reads its contents (via read_text_file) and includes them as context in the next turn, shown as a removable chip.",
    "Selecting an image attaches it ONLY when the selected model's Capabilities.vision is true; otherwise a clear visible 'this model doesn't support images' notice is shown and the image is not attached.",
    "A 📁 Repository control picks a folder, lists a bounded/filtered set of files (skipping .git/node_modules/target/etc, capped count with a truncated flag), lets the user select a subset within a total size cap, and includes the selected files' contents as context; what's included is visible and removable.",
    "Attachments and repo selections are cleared after a successful send (they apply to exactly one turn); the assembled context block is size-capped.",
    "New Tauri commands (read_text_file, read_file_base64, list_repo_files) are registered in generate_handler!, have IPC wrappers and camelCase TS types, and pass _inner unit tests (happy + oversized + missing + filtered/cap cases).",
    "run_turn/send_message backend flow is unchanged (context rides inside the existing content String); no regression to threaded chat or send-failure visibility.",
    "Frontend vitest + Rust crate tests pass; tsc/eslint/prettier clean (or CI-only with exhaustive manual review if tooling absent offline)."
  ],
  "verification": [
    "Run `cargo test -p tauri-app` for the new _inner tests IF the tauri-app crate can be built in-sandbox; it CANNOT (needs crates.io) so this gate is CI-only - do exhaustive manual self-review of the command bodies and confirm registration in lib.rs, IPC wrapper, and TS type parity. Any pure-logic helpers (path filtering, cap enforcement) that can live in a host-verifiable crate should be unit-tested there instead.",
    "Run the frontend vitest suite covering the attach + repository flows; confirm pass. If node_modules absent in-sandbox, CI-only: manually verify the dialog mock, invoke mocks, vision-gate branch, cap logic, and clear-after-send.",
    "tsc/eslint/prettier checks clean (or CI-only with manual review)."
  ],
  "blocked_reason": null,
  "findings": "DONE (committed onto feat/kiro-chat-redesign). Built on FEAT-002's composer icon slots: replaced the Attach/Repository COUNT stubs with real dialog + backend file-read wiring. Did NOT touch the web-search toggle (FEAT-004) or start LAN sharing (FEAT-006).\n\nBACKEND:\n  - NEW host-verifiable module crates/domain/src/file_context.rs (registered `pub mod file_context;` in domain/src/lib.rs): pure helpers + inline unit tests so `cargo test -p domain` covers them OFFLINE (tauri-app builds only in CI). Consts MAX_ATTACH_BYTES=256 KiB, MAX_IMAGE_BYTES=4 MiB, MAX_REPO_ENTRIES=500; SKIPPED_DIRS (.git/node_modules/target/dist/build/.venv/venv/__pycache__/.next/.cache); BINARY_EXTENSIONS list. fns file_name, extension_of, is_skipped_dir, has_binary_extension, guess_mime_type, within_text_cap, within_image_cap, encode_base64 (hand-rolled RFC 4648 so NO new crate dep). Tests: file_name separators/edges, extension lowercasing+dotfiles, skip-list, binary detection, mime guess+fallback, cap bounds, base64 RFC vectors.\n  - crates/tauri-app/src/commands.rs: 3 NEW #[tauri::command]s, each with a `_inner(&Path, display)` seam (established pattern) and camelCase-serde view structs:\n      * read_text_file(path) -> FileContentView { path, name, byteLen, text }: validates non-empty path, rejects binary-by-extension, checks metadata is_file + within_text_cap, reads, RE-checks cap post-read, String::from_utf8 (rejects non-utf8). NotFound on missing, InvalidArgument on empty/binary/oversized/non-utf8.\n      * read_file_base64(path) -> FileBinaryView { path, name, mimeType, base64, byteLen }: own 4 MiB cap, mime from extension, base64 via domain::file_context::encode_base64.\n      * list_repo_files(dir) -> RepoListing { dir, files:[RepoFileEntry{relPath,byteLen}], truncated }: explicit-stack (non-recursive) bounded walk, skips SKIPPED_DIRS + binary extensions, caps at MAX_REPO_ENTRIES with truncated flag, sorts by relPath. Stateless FS reads (no AppState/events).\n    Added `use std::path::Path;` + `use domain::file_context;` at top; MAX_FS_PATH_LEN=4096; validate_fs_path helper.\n  - crates/tauri-app/Cargo.toml: added direct `domain = { path = \"../domain\" }` dep (for file_context) with rationale comment.\n  - crates/tauri-app/src/lib.rs generate_handler!: registered read_text_file, read_file_base64, list_repo_files.\n  - Rust _inner tests added in commands.rs tests mod (uses existing tempfile dev-dep): read_text_file happy/empty+missing/oversized/non-utf8/binary-ext; read_file_base64 happy (asserts base64 'Zm9vYmFy')+missing; list_repo_files filters(.git/node_modules/binary skipped, nested rel paths)/missing-dir/truncated-at-cap.\n\nIPC + TYPES:\n  - frontend/src/ipc/commands.ts: wrappers readTextFile/readFileBase64/listRepoFiles (+ imports).\n  - frontend/src/types/index.ts: camelCase types FileContentView, FileBinaryView, RepoFileEntry, RepoListing mirroring the Rust view structs exactly.\n\nFRONTEND MECHANISM:\n  - state/conversations.ts: NEW Attachment type { kind:'text'|'repo'|'image', name, path, byteLen, text?, base64?, mimeType? }; NEW store field `attachments: Attachment[]` (init []) + addAttachment(deduped by path+kind)/removeAttachment(path,kind)/clearAttachments. sendMessage now also clears attachments on SUCCESS (kept on failure for retry), mirroring the override consume-once.\n  - NEW features/chat/attachmentContext.ts (pure, unit-tested): effectiveModelSupportsVision(models, override, pin) => override ?? pin ?? first-model, reads capabilities.vision (false when no models); MAX_CONTEXT_BYTES=128 KiB; byteLength/totalBytes; assembleContent(attachments, draft) prepends a delimited block ('### Attached file: <name>' / '### Repository file: <name>' fenced; image => '### Attached image: <name>' NOTE, NOT raw bytes), size-capped with a truncation marker, draft always preserved in full.\n  - NEW features/chat/RepoPicker.tsx: popover checkbox list, running 'X of Y KiB included' budget (data-testid repo-picker-budget), disables selection past MAX_CONTEXT_BYTES (counts usedBytes from existing attachments), truncated note, role=dialog, checkbox aria-label 'Include <relPath>'.\n  - features/chat/Composer.tsx: 📎 -> open({multiple:true, filters text+image}) then readTextFile or (image) readFileBase64 GATED on effectiveModelSupportsVision (else visible notice 'This model doesn't support images', role=status data-testid composer-notice, image NOT attached + read not called). 📁 -> open({directory:true}) -> listRepoFiles -> RepoPicker -> readTextFile per selected (kind:'repo'). Chips with remove (aria-label 'Remove attachment <name>'). submit() prepends assembleContent(attachments, trimmed draft) then sendMessage. Preserved EXACTLY: Message textarea aria-label, Send/Stop semantics, Enter/Shift+Enter IME-safe, sendState failed role=alert, web-search toggle, InlineModelControl/RoutingModeToggle/AutoRationaleTooltip/EnumerationErrorModal/Refresh.\n  - styles/app.css: .composer__notice, .composer__chips/__chip/__chip-label/__chip-remove, .composer__repo, .repo-picker* - all var(--ah-*) tokens only (NO new tokens needed).\n\nIMAGE/VISION DECISION: vision GATE + visible notice enforced. Full multimodal image bytes are NOT passed through content this iteration (run_turn maps only text content into provider history); a vision-supported image is included as a labelled NOTE. Documented as a follow-up in architecture.md 8.1.\n\nTESTS/MOCKS ADDED:\n  - chat.test.tsx: mocked @tauri-apps/plugin-dialog open() (dialogOpen), reset in beforeEach; added attachments:[] to resetStores; visionModel() helper; tests: text attach adds chip + prepends content on send; image attach w/ vision model attaches; image w/ non-vision model shows notice + NOT attached (read not called); remove chip drops it; attachments clear after send; repo picker respects cap + includes selected content on send.\n  - conversations.test.ts: attachments:[] in reset; attachment actions add(dedup)/remove/clear; clear-on-success + keep-on-failure.\n  - attachmentContext.test.ts (NEW): assembleContent no-attachments/text+repo/image-note/cap+truncation-marker; totalBytes; vision gate override>pin>first + empty.\n  - ipc/commands.test.ts: readTextFile/readFileBase64/listRepoFiles wrapper tests.\n  - app.test.tsx: routeInvoke now returns well-formed read_text_file/read_file_base64/list_repo_files shapes; attachments:[] added to ALL THREE full conversations-store resets (beforeEach + the two in-test resets).\n  - model-selector.test.tsx: attachments:[] added to resetStores (does NOT mount Composer, so no dialog mock needed).\n\nGATES: OFFLINE-VERIFIED: `cargo fmt --check --manifest-path crates/domain/Cargo.toml` and `--manifest-path crates/tauri-app/Cargo.toml` both PASS (fmt parses the files, catching syntax errors). CI-ONLY (per FEAT-001; node_modules absent + cargo cache can't resolve deps offline): cargo build/test/clippy -D warnings (domain tests + tauri-app _inner tests), frontend vitest/tsc/eslint/prettier. Self-audited manually vs the known failure classes: no unused imports (domain dep + Path + file_context all used); base64 hand-rolled to avoid a new pinned dep; every mounting test mocks the 3 new commands + plugin-dialog; attachments:[] initialized in every conversations-store reset; new inputs have explicit aria-labels (checkbox 'Include <relPath>', chip-remove 'Remove attachment <name>'); assertions after state changes use findBy*/waitFor; CSS uses only var(--ah-*) tokens.\n\nDID NOT: push/tag/PR; change run_turn/send_message backend semantics; start FEAT-004 or FEAT-006."
}
