{
  "id": "FEAT-002",
  "type": "feat",
  "description": "The Rust orchestration spine: P1.2 persistence layer (sqlx SQLite Db + versioned migrations + repositories + AppConfig with schema_version), P1.3 secrets keystore (SecretStore trait + keyring-backed impl + in-memory/mock impl for CI-safe tests, resolving SecretRef handles with no raw-key getter), P1.4 SessionManager over the repos (create/list/rename/tag/delete conversations, append messages, assign personas, per-conversation route pins + privacy tags, plus the Section 7.5 per-conversation write lock), and P1.5 CoreEvent variants. Depends on FEAT-001 (domain models must exist). These crates all gain crates.io deps (sqlx, keyring, tokio, serde, uuid, chrono, thiserror, async-trait) so they are CI-authoritative per the arrangement FEAT-001 established.",
  "status": "completed",
  "steps": [
    "Read architecture.md Sections 7.2 (persistence: SQLite via sqlx, versioned migrations, tables conversations/messages/agent_personas/mcp_servers/providers/app_config, repositories), 7.5 (concurrency: per-conversation write lock), 9.1 (secrets: keyring, SecretRef, no raw key ever returned), 10.4 (schema_version). Read the FEAT-001 findings for the chosen workspace arrangement and the SecretRef definition decision.",
    "P1.3 secrets crate: In crates/secrets/src/lib.rs define `SecretRef` (a stable string handle, serde-serializable, e.g. namespaced service+account id) and a `SecretStore` trait with methods to store a secret and receive a SecretRef, resolve a SecretRef to secret material ONLY for internal core use (return type must NOT be exposed via any public getter that could cross IPC; document this), and delete/rotate. CRITICAL: no method should return plaintext to callers outside the core's controlled use; the trait's resolve method is the single internal seam. Implement a `KeyringSecretStore` backed by the `keyring` crate (namespaced service id per Section 9.1). ALSO implement an `InMemorySecretStore` (behind a test/feature or always-available) so tests do NOT require a real OS keychain - headless Linux CI has no Secret Service and keyring tests would hang/fail. Add crates.io deps to crates/secrets/Cargo.toml (keyring, thiserror, serde) referencing [workspace.dependencies]. Add cargo tests: store then resolve a SecretRef round-trips (using InMemorySecretStore), AND a test asserting no public getter returns the plaintext key material (e.g. verify the SecretRef Debug/serde form contains only the handle, not the secret, and that there is no accessor exposing plaintext other than the documented internal resolve). Gate any keyring-backed test so it does not run on headless CI (e.g. #[ignore] or cfg), and rely on InMemory for the CI-run assertions.",
    "P1.2 persistence crate: In crates/persistence/Cargo.toml add deps sqlx (features runtime-tokio + sqlite + macros or the offline query approach; prefer runtime-agnostic sqlite feature set that compiles without a live DB - use plain query() APIs, NOT the compile-time-checked query! macros, to avoid needing DATABASE_URL at build time), tokio, serde, serde_json, uuid, chrono, thiserror, plus a path dep on orchestrator-core for the domain models. In src/db.rs implement `Db` wrapping a sqlx SqlitePool with a constructor that opens a SQLite file (and a helper for a temp/in-memory DB for tests) and runs migrations at startup. Implement versioned migrations: either sqlx::migrate! with a migrations/ dir of numbered .sql files, or an explicit versioned migration runner that records applied version in a schema_version/meta table. Create tables conversations, messages, agent_personas, mcp_servers, providers, app_config per Section 7.2 (store complex fields like privacy_tags/enabled_tool_servers/content/route/usage/transport/extra as JSON TEXT columns via serde_json). In src/repositories.rs implement ConversationRepo, PersonaRepo, and repos for messages, mcp_servers, providers with typed CRUD (insert/get/list/update/delete) mapping rows to/from the FEAT-001 domain models. Secrets are NEVER stored here - only SecretRef handles (providers table stores api_key_ref as the SecretRef handle string). In src/config.rs implement AppConfig load/save with a schema_version field (Section 10.4) persisted to app_config, with forward-compatible load (unknown newer fields preserved where feasible).",
    "P1.2 tests: add cargo tests covering migrations run cleanly against a fresh temp SQLite file, and CRUD round-trips for conversations, messages, personas, mcp_servers, providers (insert -> get -> update -> list -> delete), plus AppConfig save/load preserving schema_version. Use a temp-file or in-memory SQLite DB created in the test. These are the tasks.md acceptance for persistence.",
    "P1.4 SessionManager in orchestrator-core: In crates/orchestrator-core/src/session.rs (alongside the models, or a session_manager.rs module) implement `SessionManager` holding the repos (via the persistence crate) - add a path dep on persistence and any needed async runtime (tokio) + async-trait if used, updating orchestrator-core/Cargo.toml. Methods: create_conversation, list_conversations, rename_conversation, set_conversation_tags (privacy tags), set_conversation_route (per-conversation pin), delete_conversation, append_message, assign_persona. Implement the Section 7.5 per-conversation write lock: a map of per-conversation async locks (e.g. Mutex keyed by conversation Uuid) so writes within one conversation are serialized while different conversations proceed in parallel. Document that reads are concurrent and cross-conversation writes are not blocked.",
    "P1.5 CoreEvent in orchestrator-core/src/events.rs: replace the placeholder with a `CoreEvent` enum having exactly these variants (serde-tagged for the frontend, camelCase): message_delta, message_complete, message_error, conversation_updated, conversation_created, conversation_deleted, mcp_state_changed, mcp_error, permission_requested, providers_changed, personas_changed. Give each variant a reasonable display-safe payload (ids, deltas, statuses, rationales) referencing the domain models where appropriate; NEVER include secret material (Section 9.1/9.2 event hygiene). Add a scoped #[allow(dead_code)] with a 'wired in Phase 2+/consumed by tauri-app in FEAT-003' note ONLY on variants/fields not yet constructed, to keep clippy -D warnings clean.",
    "P1.4 tests: add orchestrator-core cargo tests covering session lifecycle: create a conversation, append messages, assign a persona, and exercise the per-conversation write lock (e.g. concurrent appends to the same conversation are serialized and both persist; appends to different conversations can proceed without deadlock). Use an in-memory/temp SQLite Db from the persistence crate.",
    "Update or remove the Phase 0 placeholder()/smoke tests in persistence, secrets, and orchestrator-core now that real APIs exist; keep providers/mcp-client/routing placeholders intact if orchestrator-core's smoke test still references them, or update that smoke test accordingly. Ensure no dangling references.",
    "Author all files with LF line endings. Run cargo fmt over these crates. Because these crates require crates.io deps unreachable offline, they will NOT build in the sandbox - do NOT attempt network installs. Verify code is syntactically consistent as far as offline tooling allows, and record clearly in findings that persistence/secrets/orchestrator-core compilation + tests are DEFERRED TO CI (network). Ensure the CI workflow from FEAT-001 builds+tests+clippies these three crates explicitly."
  ],
  "acceptance_criteria": [
    "secrets crate defines SecretStore trait + KeyringSecretStore + InMemorySecretStore; SecretRef handles are returned/resolved; a test asserts no public getter returns plaintext key material; keyring-backed tests are gated so CI-run tests use the in-memory store and require no OS keychain.",
    "persistence crate implements Db (sqlx SqlitePool) with versioned migrations creating conversations/messages/agent_personas/mcp_servers/providers/app_config tables, typed repositories with CRUD, and AppConfig load/save with schema_version; secrets are stored only as SecretRef handles.",
    "orchestrator-core SessionManager implements create/list/rename/tag/route-pin/delete conversations, append messages, assign personas, with the Section 7.5 per-conversation write lock.",
    "orchestrator-core CoreEvent enum defines exactly the eleven listed variants with display-safe (no-secret) payloads.",
    "cargo tests exist for: persistence migrations + CRUD round-trips against a temp SQLite file; secrets store/resolve + no-plaintext-leak; orchestrator-core session lifecycle incl. per-conversation lock.",
    "CI workflow builds+tests+clippies persistence, secrets, orchestrator-core explicitly; clippy -D warnings clean (dead_code allows scoped + annotated where unavoidable).",
    "Files use LF line endings; what cannot build offline is documented as deferred-to-CI in findings."
  ],
  "verification": [
    "These three crates require crates.io deps and CANNOT be verified offline; primary verification is CI. Do NOT attempt network installs.",
    "Offline: run `cargo fmt --check` on the changed crates if the toolchain allows formatting without dep resolution; record result.",
    "Confirm the CI workflow contains explicit build+test+clippy(-D warnings) steps for persistence, secrets, and orchestrator-core.",
    "Read through the secrets tests to confirm one test would FAIL if a plaintext getter were added (i.e. it meaningfully asserts non-exposure), and that keyring tests are gated off headless CI.",
    "Read the persistence tests to confirm they run migrations then perform real insert/get/update/delete against a temp SQLite DB (not static asserts)."
  ],
  "blocked_reason": null,
  "findings": "IMPLEMENTED\n- P1.3 secrets (crates/secrets/src/lib.rs): canonical `SecretRef` newtype (serde-serializable, `Serialize`/`Deserialize` as the bare handle string, plus `new()`/`handle()`), `KEYCHAIN_SERVICE` namespaced service id (Section 9.1), `SecretError` (thiserror), and the `SecretStore` trait with `store`/`resolve`/`delete`/`rotate`. `resolve` is documented as the SINGLE core-internal plaintext seam (never wired to a Tauri command). Two impls: `KeyringSecretStore` (backed by `keyring::Entry` under KEYCHAIN_SERVICE, maps NoEntry->NotFound) and `InMemorySecretStore` (Mutex<HashMap>, always available for tests/headless CI). Cargo.toml gained keyring/serde/thiserror + dev-dep serde_json.\n- SecretRef reconciliation: the canonical `SecretRef` now lives in `secrets`; orchestrator-core `models.rs` REPLACED its local newtype with `pub use secrets::SecretRef;` (single source of truth). Dependency direction stays orchestrator-core -> secrets (secrets has zero path deps). The pre-existing models.rs test still constructs `SecretRef(\"...\".into())` (tuple field is pub) and the provider_config test still passes.\n- P1.2 persistence: Cargo.toml gained sqlx 0.8 (features runtime-tokio, sqlite, chrono, uuid, migrate), tokio, serde/serde_json/uuid/chrono/thiserror, path dep orchestrator-core, dev-dep tempfile. migrations/0001_initial.sql creates conversations/messages/agent_personas/mcp_servers/providers/app_config with structured fields as JSON TEXT (privacy_tags, enabled_tool_servers, content, route, usage, transport, parameters, extra); Uuids/timestamps as TEXT; messages FK -> conversations ON DELETE CASCADE; app_config single-row (id=1) with a dedicated schema_version INTEGER column + JSON data blob. db.rs: `Db` wraps SqlitePool; `open(path)` (create_if_missing + foreign_keys on), `open_in_memory()` (single-connection shared :memory: for tests), `run_migrations()` via `sqlx::migrate!(\"./migrations\")` (idempotent). Uses runtime `sqlx::query()` APIs only - NO `query!` macros - so no DATABASE_URL at build time. repositories.rs: ConversationRepo/MessageRepo/PersonaRepo/McpServerRepo/ProviderRepo with insert/get/list/update/delete mapping rows<->FEAT-001 domain models via serde_json; ProviderRepo persists ONLY the SecretRef handle string in api_key_ref (never key material). config.rs: `AppConfig` (schema_version + typed known fields + `#[serde(flatten)]` extra catch-all for forward-compat per Section 10.4) with load()/save() upsert; the dedicated schema_version column is authoritative on load.\n- P1.4 SessionManager (orchestrator-core/src/session.rs): holds a `Db` + per-conversation async write locks (`Arc<StdMutex<HashMap<Uuid, Arc<tokio::Mutex<()>>>>>`; outer std mutex guards only the map, inner tokio mutex held across the async write). Methods: create_conversation(ConversationInit), list_conversations, get_conversation, rename_conversation, set_conversation_tags, set_conversation_route (pin), assign_persona, delete_conversation, append_message (bumps updated_at, requires existing conversation), list_messages, personas(). Section 7.5 documented: reads are concurrent/unlocked; writes serialized per-conversation; distinct conversations proceed in parallel; delete drops its lock entry. orchestrator-core Cargo.toml gained tokio. lib.rs re-exports SessionManager/ConversationInit/SessionError and CoreEvent/McpConnectionState; the smoke test now only calls providers/mcp-client/routing placeholder() (persistence/secrets no longer expose placeholder()).\n- P1.5 CoreEvent (orchestrator-core/src/events.rs): serde `#[serde(tag=\"type\", rename_all=\"camelCase\")]` enum with EXACTLY 11 variants: messageDelta, messageComplete, messageError, conversationUpdated, conversationCreated, conversationDeleted, mcpStateChanged, mcpError, permissionRequested, providersChanged, personasChanged. Display-safe payloads only (ids/deltas/statuses/rationales; McpConnectionState helper enum) - no secrets/credentials (Section 9.1/9.2). Scoped `#[allow(dead_code)]` with a 'constructed by pipeline / tauri-app bridge in later Phase 1/2 (FEAT-003)' note on the enum, to keep clippy -D warnings clean until variants are constructed.\n- TS mirror (frontend/src/types/index.ts): added `McpConnectionState` and the `CoreEvent` discriminated union mirroring events.rs (camelCase, tagged on `type`). SecretRef mirror was already present from FEAT-001 (unchanged - still `string`).\n- Workspace arrangement (hybrid-orchestrator/Cargo.toml): MOVED persistence and secrets from `members` to `[workspace] exclude` (they now carry offline-unreachable crates.io deps), joining orchestrator-core and tauri-app. members now = providers, mcp-client, routing only. Extended the Phase-0-style rationale (which crates are excluded and why, the KNOWN TRADEOFF, and the EXIT PATH now naming all four excluded crates). Noted that excluded crates pin concrete literal versions (kept in sync with [workspace.dependencies]) because an excluded crate cannot use `{ workspace = true }`. persistence/secrets Cargo.toml de-inherit workspace keys to concrete literals (mirror the tauri-app / orchestrator-core precedent).\n- CI (.github/workflows/ci.yml): updated header (lists persistence/secrets as excluded + a SECRETS-ON-HEADLESS-CI note), added fmt-check steps for persistence and secrets, and added explicit build/clippy(-D warnings)/test steps for secrets and persistence (before the orchestrator-core steps, since core path-depends on them). tauri-app + orchestrator-core steps retained; frontend + tauri build retained.\n- Removed the Phase-0 placeholder()/smoke stubs from persistence and secrets (replaced by real APIs + their own tests). providers/mcp-client/routing placeholders kept (orchestrator-core smoke test still references them).\n\nTESTS ADDED (all authored; run in CI - see DEFERRED)\n- secrets: store_then_resolve_round_trips, rotate_keeps_ref_stable_and_replaces_material, delete_is_idempotent_and_removes_material, secret_ref_never_exposes_plaintext (asserts handle()/Debug/serde form contain the handle but NOT the plaintext - would FAIL if a plaintext getter/leak were added), and keyring_store_round_trips (`#[ignore]`d: requires a real OS keychain, so headless Linux CI skips it; CI-run assertions use InMemory).\n- persistence: conversation/message/persona/mcp_server/provider CRUD round-trips (insert->get->update->list->delete) against an in-memory SQLite Db (migrations run via Db::open_in_memory), provider test also asserts the raw api_key_ref column stores only the handle; config load_defaults_when_empty, save_then_load_preserves_schema_version_and_fields, unknown_newer_fields_are_preserved_on_load.\n- orchestrator-core: session_lifecycle_create_append_assign (create/list/rename/tag/route-pin/append/persona-assign/delete), appends_to_same_conversation_are_all_persisted (8 concurrent appends serialized by the per-conversation lock, all persist), writes_to_different_conversations_do_not_deadlock (parallel cross-conversation writes complete). events: camelCase tagged-union serialization + permissionRequested round-trip.\n\nOFFLINE-VERIFIED (green in sandbox)\n- cargo build --workspace (members: providers/mcp-client/routing) -> OK.\n- cargo test --workspace -> OK. cargo clippy --workspace --all-targets -- -D warnings -> OK.\n- cargo fmt: --all --check clean; --manifest-path check clean for orchestrator-core, persistence, secrets, tauri-app (ran cargo fmt on core+persistence to normalize).\n- cargo verify-project for orchestrator-core, persistence, secrets -> all {\"success\":\"true\"} (manifests parse).\n- All new/modified files confirmed LF (no CRLF).\n- Cargo.lock: persistence + secrets dropped out of the offline workspace lockfile (expected; they are no longer members, same as orchestrator-core/tauri-app). Committed.\n\nDEFERRED TO CI (authoritative gate; cannot run offline - IDENTICAL to how orchestrator-core/tauri-app already behave)\n- cargo build/test/clippy for secrets, persistence, and orchestrator-core FAIL OFFLINE ONLY at crates.io dependency resolution (verified: `cargo build --manifest-path crates/secrets/Cargo.toml` errors with HTTP 403 downloading the crates.io index) - a NETWORK failure, NOT a code/compile error. CI (network, ubuntu/macOS/windows) compiles, tests, and clippies all three via --manifest-path. The 15 unit/integration tests above (secrets 4 active + 1 ignored, persistence 8, orchestrator-core session 3 + events 2) run there. The keyring-backed test is #[ignore]d so headless Linux CI needs no Secret Service.\n- TS types: no offline tsc (no node_modules; npm 403). The CoreEvent/McpConnectionState additions were hand-mirrored against events.rs; type-checked in CI vitest/tsc. camelCase field names verified by manual diff against the serde attributes.\n\nNOTES FOR SUBSEQUENT FEATURES (FEAT-003)\n- The CoreEvent enum carries a scoped `#[allow(dead_code)]`; FEAT-003 wires the tauri-app event bridge to construct/forward these variants and should REMOVE the allow once every variant is constructed.\n- SessionManager is the seam the Tauri command surface should call; it is `Clone` (cheap: Db pool + Arc lock map) so it can live in Tauri managed state.\n- `SecretStore::resolve` is the only plaintext seam and MUST NOT be exposed via any #[tauri::command]; the settings flow's set_provider_secret should call `store()` and return only the SecretRef (Section 9.1)."
}
